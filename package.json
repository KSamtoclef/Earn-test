{
  "name": "earn-chat-production",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-production.mjs",
    "build": "npm run validate && node scripts/build-static.mjs",
    "test": "npm run validate"
  }
}
