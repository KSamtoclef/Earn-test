import fs from'node:fs';
const path='assets/js/admin/core.js';
let source=fs.readFileSync(path,'utf8');
const required=(from,to,label)=>{if(!source.includes(from))throw new Error(`Admin consolidation could not find: ${label}`);source=source.replace(from,to)};
required('let usersCache=[];','let usersCache=[],renderToken=0;','render token');
required('Frontend release: <b>20260731-production-certification-r1</b>','Frontend release: <b>20260801-source-consolidated-r1</b>','Admin release label');
required("await api.adminSaveTask($('#tf-id').value||null,payload);await tasks()","await api.adminSaveTask($('#tf-id').value||null,payload);document.dispatchEvent(new CustomEvent('earnchat:form-save-succeeded',{detail:{form}}));await tasks()",'task save event');
required("await api.adminUpdateKycConfig({enabled:$('#kc-enabled').checked,provider_ng:$('#kc-provider-ng').value.trim(),provider_ke:$('#kc-provider-ke').value.trim(),url_ng:ng,url_ke:ke,instructions_ng:$('#kc-ins-ng').value.trim(),instructions_ke:$('#kc-ins-ke').value.trim(),reference_required:$('#kc-reference').value==='true',review_hours:Number($('#kc-hours').value||48)});alert('KYC configuration saved.')","await api.adminUpdateKycConfig({enabled:$('#kc-enabled').checked,provider_ng:$('#kc-provider-ng').value.trim(),provider_ke:$('#kc-provider-ke').value.trim(),url_ng:ng,url_ke:ke,instructions_ng:$('#kc-ins-ng').value.trim(),instructions_ke:$('#kc-ins-ke').value.trim(),reference_required:$('#kc-reference').value==='true',review_hours:Number($('#kc-hours').value||48)});document.dispatchEvent(new CustomEvent('earnchat:form-save-succeeded',{detail:{form:e.target}}));alert('KYC configuration saved.')",'KYC save event');
required("await api.adminSaveFeedback(null,$('#fb-quote').value.trim(),$('#fb-country').value,$('#fb-verified').checked,$('#fb-visible').checked);await payments()","await api.adminSaveFeedback(null,$('#fb-quote').value.trim(),$('#fb-country').value,$('#fb-verified').checked,$('#fb-visible').checked);document.dispatchEvent(new CustomEvent('earnchat:form-save-succeeded',{detail:{form:e.target}}));await payments()",'feedback save event');
required("await api.adminUpdateBusiness(Object.fromEntries([...new FormData(e.target).entries()].map(([k,v])=>[k,Number(v)])));await configuration()","await api.adminUpdateBusiness(Object.fromEntries([...new FormData(e.target).entries()].map(([k,v])=>[k,Number(v)])));document.dispatchEvent(new CustomEvent('earnchat:form-save-succeeded',{detail:{form:e.target}}));await configuration()",'business save event');
required("await api.adminUpdateLevel(f.dataset.level,Object.fromEntries([...new FormData(f).entries()].map(([k,v])=>[k,Number(v)])));await configuration()","await api.adminUpdateLevel(f.dataset.level,Object.fromEntries([...new FormData(f).entries()].map(([k,v])=>[k,Number(v)])));document.dispatchEvent(new CustomEvent('earnchat:form-save-succeeded',{detail:{form:f}}));await configuration()",'level save event');
required("export async function renderAdmin(){renderNavigation();loading();try{if(!await api.isAdmin())throw new Error('Administrator permission required');await loadUsers();const renderers={overview,live,users,tasks,claims,chats,referrals,qualifications,withdrawals,kyc,payments,configuration,analytics,audit};await renderers[tab]()}catch(error){fail(error)}}","export async function renderAdmin(){const token=++renderToken,current=tab;renderNavigation();loading();try{if(!await api.isAdmin())throw new Error('Administrator permission required');const userTabs=new Set(['live','users','claims','chats','referrals','qualifications','withdrawals','kyc','analytics','audit']);if(userTabs.has(current))await loadUsers();if(token!==renderToken)return;const renderers={overview,live,users,tasks,claims,chats,referrals,qualifications,withdrawals,kyc,payments,configuration,analytics,audit};await renderers[current]();if(token!==renderToken){queueMicrotask(()=>renderAdmin());return}document.dispatchEvent(new CustomEvent('earnchat:form-rendered',{detail:{root:host()}}))}catch(error){if(token===renderToken)fail(error)}}",'Admin route render ownership');
fs.writeFileSync(path,source);
