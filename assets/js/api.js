import{sb}from'./supabase-client.js';
const unwrap=r=>{if(r.error)throw r.error;return r.data};
const rpc=(name,args={})=>sb.rpc(name,args).then(unwrap);
const select=(table,columns='*')=>sb.from(table).select(columns);
export const api={
 session:async()=>unwrap(await sb.auth.getSession()),
 signup:async(email,password,fullName,country)=>unwrap(await sb.auth.signUp({email,password,options:{data:{full_name:fullName,country}}})),
 login:async(email,password)=>unwrap(await sb.auth.signInWithPassword({email,password})),
 logout:async()=>unwrap(await sb.auth.signOut()),
 ensureProfile:async(fullName,country)=>rpc('ensure_earnchat_profile',{p_full_name:fullName||null,p_country:country||null}),
 state:async()=>rpc('get_my_earnchat_state'),
 business:async()=>rpc('get_earnchat_business_config'),
 publicStats:async()=>rpc('get_public_earnchat_stats'),
 payments:async()=>unwrap(await select('earnchat_payment_activity','masked_name,country_code,amount,currency,payout_method,paid_at').eq('is_visible',true).eq('is_verified',true).order('paid_at',{ascending:false}).limit(2)),
 feedback:async()=>unwrap(await select('earnchat_member_feedback','quote,country_code').eq('is_visible',true).eq('verified_paid_member',true).order('created_at',{ascending:false}).limit(3)),
 tasks:async()=>rpc('list_earnchat_tasks'),
 startTask:async id=>rpc('start_earnchat_task',{p_task:id}),
 submitTask:async(id,proof={})=>rpc('submit_earnchat_task',{p_claim:id,p_proof:proof}),
 openTaskClaim:async()=>rpc('get_my_open_task_claim'),
 startChat:async partner=>rpc('start_earnchat_chat',{p_partner:partner||null}),
 completeChat:async(attempt,replies,quality={})=>rpc('complete_earnchat_chat',{p_attempt:attempt,p_replies:replies,p_quality:quality}),
 openChatAttempt:async()=>rpc('get_my_open_chat_attempt'),
 referrals:async userId=>unwrap(await select('earnchat_referrals').eq('referrer_id',userId).order('signup_at',{ascending:false})),
 registerReferral:async code=>rpc('register_earnchat_referral',{p_code:code}),
 qualificationMissions:async()=>unwrap(await select('earnchat_qualification_missions').eq('status','active').order('created_at',{ascending:false})),
 qualificationSubmissions:async userId=>unwrap(await select('earnchat_qualification_submissions').eq('user_id',userId).order('started_at',{ascending:false})),
 startQualification:async id=>rpc('start_earnchat_qualification',{p_mission:id}),
 submitQualification:async(id,proof)=>rpc('submit_earnchat_qualification',{p_submission:id,p_proof:proof||{}}),
 kycConfig:async()=>rpc('get_earnchat_kyc_config'),
 submitKyc:async(reference,metadata={})=>rpc('submit_earnchat_kyc',{p_reference:reference||null,p_metadata:metadata}),
 withdraw:async(wallet,amount,method,payout)=>rpc('request_earnchat_withdrawal',{p_wallet:wallet,p_amount:amount,p_method:method,p_payout:payout}),
 ledger:async userId=>unwrap(await select('earnchat_ledger').eq('user_id',userId).order('created_at',{ascending:false}).limit(100)),
 withdrawals:async userId=>unwrap(await select('earnchat_withdrawals').eq('user_id',userId).order('created_at',{ascending:false})),
 event:async(name,session,page,metadata={})=>rpc('record_earnchat_event',{p_event:name,p_session:session||null,p_page:page||null,p_metadata:metadata}),
 isAdmin:async()=>rpc('earnchat_is_admin'),
 adminOverview:async()=>rpc('admin_get_earnchat_overview'),
 adminTasks:async()=>rpc('admin_list_earnchat_tasks'),
 adminSaveTask:async(id,payload)=>rpc('admin_upsert_earnchat_task',{p_id:id||null,p_payload:payload}),
 adminDeleteTask:async id=>rpc('admin_delete_earnchat_task',{p_task:id}),
 adminClaims:async(limit=200,offset=0)=>unwrap(await select('earnchat_task_claims').order('submitted_at',{ascending:false}).range(offset,offset+limit-1)),
 adminReviewClaim:async(id,decision,reason)=>rpc('admin_review_task_claim',{p_claim:id,p_decision:decision,p_reason:reason||null}),
 adminReverseClaim:async(id,reason)=>rpc('admin_reverse_task_claim',{p_claim:id,p_reason:reason||'Administrator reversal'}),
 adminBulkReviewClaims:async(ids,decision,reason)=>rpc('admin_bulk_review_task_claims',{p_claims:ids,p_decision:decision,p_reason:reason||null}),
 adminKyc:async(limit=200,offset=0)=>unwrap(await select('earnchat_kyc_submissions').order('created_at',{ascending:false}).range(offset,offset+limit-1)),
 adminKycConfig:async()=>rpc('get_earnchat_kyc_config'),
 adminUpdateKycConfig:async payload=>rpc('admin_update_earnchat_kyc_config',{p_payload:payload}),
 adminReviewKyc:async(id,decision,reason)=>rpc('admin_review_earnchat_kyc',{p_submission:id,p_decision:decision,p_reason:reason||null}),
 adminBulkReviewKyc:async(ids,decision,reason)=>rpc('admin_bulk_review_earnchat_kyc',{p_submissions:ids,p_decision:decision,p_reason:reason||null}),
 adminWithdrawals:async(limit=200,offset=0)=>unwrap(await select('earnchat_withdrawals').order('created_at',{ascending:false}).range(offset,offset+limit-1)),
 adminReviewWithdrawal:async(id,status,reason,reference,publish)=>rpc('admin_review_earnchat_withdrawal',{p_withdrawal:id,p_status:status,p_reason:reason||null,p_reference:reference||null,p_publish:!!publish}),
 adminUsers:async(limit=200,offset=0)=>unwrap(await select('profiles','id,email,full_name,country,currency,level_name,active_days_count,approved_chats_count,approved_tasks_count,task_rejection_count,chat_rejection_count,work_available_balance,work_pending_balance,referral_available_balance,referral_pending_balance,total_withdrawn,kyc_status,security_review_required,fraud_review_status,earning_suspended,is_admin,last_visit_at,account_created_at').order('account_created_at',{ascending:false}).range(offset,offset+limit-1)),
 adminUserControl:async(userId,action,reason)=>rpc('admin_update_earnchat_user_control',{p_user:userId,p_action:action,p_reason:reason||null}),
 adminBulkUserControl:async(ids,action,reason)=>rpc('admin_bulk_update_user_control',{p_users:ids,p_action:action,p_reason:reason||null}),
 adminPresence:async()=>unwrap(await select('earnchat_site_presence').gt('last_seen',new Date(Date.now()-90000).toISOString()).order('last_seen',{ascending:false})),
 adminChats:async(limit=200,offset=0)=>unwrap(await select('earnchat_chat_sessions').order('completed_at',{ascending:false}).range(offset,offset+limit-1)),
 adminReverseChat:async(id,reason)=>rpc('admin_reverse_earnchat_chat',{p_session:id,p_reason:reason||'Administrator reversal'}),
 adminReferrals:async(limit=200,offset=0)=>unwrap(await select('earnchat_referrals').order('signup_at',{ascending:false}).range(offset,offset+limit-1)),
 adminReviewReferral:async(id,decision,reason)=>rpc('admin_review_earnchat_referral',{p_referral:id,p_decision:decision,p_reason:reason||null}),
 adminQualifications:async()=>unwrap(await select('earnchat_qualification_missions').order('created_at',{ascending:false})),
 adminQualificationSubmissions:async(limit=200,offset=0)=>unwrap(await select('earnchat_qualification_submissions').order('submitted_at',{ascending:false}).range(offset,offset+limit-1)),
 adminSaveQualification:async(id,payload)=>rpc('admin_upsert_earnchat_qualification',{p_id:id||null,p_payload:payload}),
 adminReviewQualification:async(id,decision,reason)=>rpc('admin_review_earnchat_qualification',{p_submission:id,p_decision:decision,p_reason:reason||null}),
 adminPayments:async(limit=200,offset=0)=>unwrap(await select('earnchat_payment_activity').order('paid_at',{ascending:false}).range(offset,offset+limit-1)),
 adminSetPaymentVisibility:async(id,visible)=>rpc('admin_set_payment_visibility',{p_payment:id,p_visible:!!visible}),
 adminFeedback:async(limit=200,offset=0)=>unwrap(await select('earnchat_member_feedback').order('created_at',{ascending:false}).range(offset,offset+limit-1)),
 adminSaveFeedback:async(id,quote,country,verified,visible)=>rpc('admin_upsert_earnchat_feedback',{p_id:id||null,p_quote:quote,p_country:country,p_verified:!!verified,p_visible:!!visible}),
 adminAudit:async(limit=200,offset=0)=>unwrap(await select('earnchat_admin_audit').order('created_at',{ascending:false}).range(offset,offset+limit-1)),
 adminAnalytics:async(limit=200,offset=0)=>unwrap(await select('earnchat_analytics_events').order('created_at',{ascending:false}).range(offset,offset+limit-1)),
 adminUpdateBusiness:async payload=>rpc('admin_update_earnchat_business_settings',{p_payload:payload}),
 adminUpdateLevel:async(level,payload)=>rpc('admin_update_earnchat_level',{p_level:level,p_payload:payload}),
 presence:async payload=>rpc('upsert_earnchat_presence',payload),
 presenceInactive:async sessionId=>rpc('mark_earnchat_presence_inactive',{p_session_id:sessionId})
};
