import{api}from'../api.js';
import{COUNTRY_FALLBACK,money}from'../app-config.js';

const $=(selector,root=document)=>root.querySelector(selector);
const ORDER=['Starter','Active','Pro','Elite'];
let loading=false,lastRender=0;

function daysSince(value){const t=Date.parse(value||'');return Number.isFinite(t)?Math.max(0,Math.floor((Date.now()-t)/86400000)):0}
function countryAmount(base,country,multiplier){return Math.round(Number(base||0)*(country==='KE'?Number(multiplier||.6):1))}
function requirement(label,current,target){const safeTarget=Math.max(0,Number(target||0));const safeCurrent=Math.max(0,Number(current||0));return{label,current:safeCurrent,target:safeTarget,done:safeTarget===0||safeCurrent>=safeTarget,pct:safeTarget===0?100:Math.min(100,Math.round(safeCurrent/safeTarget*100))}}
function kycScore(status,required){if(required==='none')return requirement('KYC requirement',1,1);if(required==='submitted')return requirement('Submit KYC',status==='submitted'||status==='approved'?1:0,1);return requirement('Approved KYC',status==='approved'?1:0,1)}
function nextLevel(current){const index=ORDER.indexOf(current);return index<0?'Active':ORDER[Math.min(ORDER.length-1,index+1)]}
function levelReward(config,name,country){const level=config?.levels?.[name]||{};return money(countryAmount(level.chat_reward_ngn,country,config?.settings?.kenya_multiplier),country)}
function progressData(state){const profile=state.profile||{},config=state.config||{},current=profile.level_name||'Starter',next=nextLevel(current),target=config.levels?.[next]||{},country=profile.country||'NG';const requirements=[requirement('Account days',daysSince(profile.account_created_at),target.account_days),requirement('Active days',profile.active_days_count,target.active_days),requirement('Approved chats',profile.approved_chats_count,target.approved_chats),requirement('Approved tasks',profile.approved_tasks_count,target.approved_tasks),kycScore(profile.kyc_status,target.kyc_requirement)];const required=requirements.filter(x=>x.target>0),score=required.length?Math.round(required.reduce((sum,item)=>sum+item.pct,0)/required.length):100;return{profile,config,current,next,target,country,requirements,score,isTop:current==='Elite'}}
function ensureStyles(){if(document.querySelector('link[data-level-journey]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='./assets/css/level-chat-experience.css?v=20260731-level-chat-r1';link.dataset.levelJourney='1';document.head.appendChild(link)}
function renderCard(data,compact=false){if(data.isTop)return`<article class="level-journey-card elite"><div><span class="eyebrow">HIGHEST LEVEL</span><h2>Elite unlocked</h2><p>You have reached the highest Earn Chat level. Keep your activity quality strong to retain access.</p></div><div class="level-reward"><small>CHAT REWARD</small><b>${levelReward(data.config,'Elite',data.country)}</b><span>Up to ${Number(data.target.chat_limit||10)} chats daily</span></div></article>`;const reward=levelReward(data.config,data.next,data.country),requirements=data.requirements.map(item=>`<div class="level-requirement ${item.done?'done':''}"><span>${item.done?'✓':'○'}</span><div><b>${item.label}</b><small>${Math.min(item.current,item.target)} of ${item.target}</small></div></div>`).join('');return`<article class="level-journey-card ${compact?'compact':''}"><header><div><span class="eyebrow">NEXT LEVEL</span><h2>Unlock ${data.next}</h2><p>Earned through genuine activity—no payment required.</p></div><div class="level-reward"><small>CHAT REWARD</small><b>${reward}</b><span>${Number(data.target.chat_limit||0)} chats daily</span></div></header><div class="level-score"><div><b>${data.score}% ready</b><span>${data.current} → ${data.next}</span></div><div class="progress"><i style="width:${data.score}%"></i></div></div><div class="level-requirements">${requirements}</div><button class="primary level-action" type="button" data-go="${data.requirements.find(x=>!x.done)?.label.includes('KYC')?'profile':'tasks'}">${data.score===100?'Requirements complete—refresh status':'Continue earning toward '+data.next}</button></article>`}
function placeHome(data){const home=$('#view-home .container');if(!home)return;let card=$('#home-level-journey');if(!card){card=document.createElement('div');card.id='home-level-journey';const target=$('#home-referral-priority')||$('.next-action',home);target?.insertAdjacentElement('afterend',card)}card.innerHTML=renderCard(data,true)}
function placeProfile(data){const profile=$('#view-profile .container');if(!profile)return;let card=$('#profile-level-journey');if(!card){card=document.createElement('div');card.id='profile-level-journey';const hero=$('.profile-hero',profile);hero?.insertAdjacentElement('afterend',card)}card.innerHTML=renderCard(data,false)}
async function enhance(){if(loading||Date.now()-lastRender<700)return;const hash=location.hash;if(!hash.includes('home')&&!hash.includes('profile'))return;loading=true;try{const state=await api.state();const data=progressData(state);placeHome(data);placeProfile(data);lastRender=Date.now()}catch{}finally{loading=false}}
function schedule(){[80,350,900].forEach(delay=>setTimeout(enhance,delay))}

ensureStyles();
window.addEventListener('hashchange',schedule);
window.addEventListener('pageshow',schedule);
document.addEventListener('visibilitychange',()=>{if(!document.hidden)schedule()});
schedule();
